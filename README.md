# To-Do List REST API (Laravel)

Тестовое задание: разработка простого REST API для управления списком задач (To-Do List)  
Реализовано на **Laravel** с соблюдением принципов **SOLID** и **Dependency Injection (DI)**.

Проект полностью разворачивается через **Docker** и использует **MySQL** в качестве базы данных.

---

## 📦 Стек технологий

- PHP 8.3
- Laravel
- MySQL 8
- Docker / Docker Compose
- PHPUnit (Feature + Unit тесты)

---

## 📂 Архитектура и принципы

Проект построен с разделением ответственности по слоям:

- **Controller** HTTP слой (приём запроса, возврат ответа)
- **Service** бизнес-логика (use-cases)
- **Repository** доступ к данным
- **DTO** перенос данных между слоями
- **FormRequest** валидация входных данных
- **Resource** формирование JSON-ответов
- **Contracts (Interfaces)** для строгого DI и DIP

Используемые принципы:
- Single Responsibility Principle (SRP)
- Dependency Inversion Principle (DIP)
- Dependency Injection (DI)

---

## 🚀 Запуск проекта (Docker)

### 1️⃣ Клонировать репозиторий
```bash
git clone https://github.com/itech-programmer/laravel-to-do-list.git
cd laravel-to-do-list
make start
